using Gtk 4.0;
using Adw 1;

template $PebblesWindow: Gtk.ApplicationWindow {
    default-width: 800;
    default-height: 600;
    width-request: 400;
    height-request: 420;
    title: _("Pebbles");
}
